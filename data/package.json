{
  "dependencies": {
    "pg": "^8.11.3",
    "sax": "^1.3.0"
  },
  "scripts": {
    "process": "ts-node process.ts",
    "norway_data:dump": "docker exec -i postgis /usr/bin/pg_dump -t norway_roads --user postgres > norway.sql.dump"
  },
  "devDependencies": {
    "@types/pg": "^8.10.9",
    "@types/sax": "^1.2.7",
    "ts-node": "^10.9.1"
  }
}
